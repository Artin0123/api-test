# 複製成 .env 後填入實際值。.env 已列入 .gitignore，不會進版控。
#   cp .env.example .env
#
# local_test/ollama_chat_all_models.py 使用
OLLAMA_API_KEY=

# 其他本機金鑰放在 valid_keys/（整個資料夾已 gitignore），
# Wrangler 的本機密鑰放在 .dev.vars（同樣已 gitignore）。
