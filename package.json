{
  "name": "api-test",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --kv=KV_STORE",
    "check": "node --check public/_worker.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
